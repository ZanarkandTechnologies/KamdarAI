---
kind: goal-program
ticket_id: TASK-0001
status: superseded
created_at: 2026-08-21T10:53:08+08:00
compiled_from_ticket_updated_at: 2026-08-21T10:59:23+08:00
generated_prompt: tickets/TASK-0001/artifacts/native-goal-prompt.md
approval: approved
---

# TASK-0001 Goal Program

## Goal Mode

- `mode:` `active_goal`
- `trigger:` native Goal, bounded autonomous execution
- `files:` ticket, program, latest progress, workspace context, three automation
  contracts, and live connector receipts
- `budget:` no spend/deploy/public-share budget; live writes only inside ticket
  authority

## Metric Provider

- `provider:` frozen deterministic checks plus live provider postconditions and
  independent browser, eval-quality, setup, drift, and completion judgment
- `direction:` pass/fail by ticket Done / Proof
- `guards:` factual sources, exact contact allowlist, namespaced writes, no
  secrets/private data in tracked artifacts, no production activation
- `anti_metrics:` file/assertion count or visual polish without operated proof

## Decision Backbone

`observe -> choose_next(objective, evidence, eligible_moves, remaining_budget) -> execute | diagnose | report_now | request_feedback | stop -> act -> verify -> write_back`

Prefer the mechanically implied unresolved Done condition. Keep the frozen
processor network-free; provider adapters own live reads/writes at the edge.

## Proof Policy

- `critical_path:` provider preflight -> bounded source snapshot -> namespaced
  POC seed -> shared transform -> area reports -> company rollup -> Notion/Drive
  writes -> exactly two Gmail sends -> configured-home Telegram send -> provider
  postchecks -> scored receipt -> UI Results -> showcase
- `proof_weight:` deterministic + provider_receipts + qa + visual_qa + agent_qa
  + demo + drift_review + completion_review
- `self_certification:` prohibited for final UI, eval quality, and completion

## Context / Logging

Initial load is full ticket/program and latest 80 progress lines; target 300
lines and hard-block above 400. Append observation, evidence, learning,
decision, remaining budget, and next action after each material phase. Regenerate
packet when scope changes.

## Side-Effect Program

1. Run redacted auth/read probes.
2. Resolve exact Notion parent, Drive parent, Gmail identity, Telegram home.
3. Create or reuse only exact `[POC] Kamdar Manager Eval 2026-08-21` resources.
4. Verify targets before mutation and record returned IDs only in ignored runs.
5. Send Gmail only to the runtime two-address allowlist; send Telegram only to
   profile home; verify returned message receipts.
6. Never delete live POC evidence in this Goal; recovery is leave namespaced
   resources intact and stop further writes.

## Stop Conditions

- `complete_when:` all Done / Proof conditions and independent gates pass.
- `stop_now:` secret exposure, target mismatch, non-POC overwrite, unapproved
  recipient, destructive action, production schedule/gateway change, or scope
  drift beyond the single scenario.
- `blocked_when:` same non-inferable environment gap prevents progress for three
  Goal turns; a single unavailable provider is honestly `blocked`, not mocked.

## Final Checkpoint

Run ordered sanity checks, provider postchecks, critical-path QA, visual proof,
agent/eval QA, demo, drift review, and completion review. Write evidence and the
reviewed response to ticket artifacts before `stop_complete`.
