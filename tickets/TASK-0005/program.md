---
kind: goal-program
ticket_id: TASK-0005
status: complete
created_at: 2026-08-21T18:00:00+08:00
compiled_from_ticket_updated_at: 2026-08-21T17:42:00+08:00
generated_prompt: tickets/TASK-0005/artifacts/native-goal-prompt.md
approval: approved-by-operator-2026-08-21
completed_at: 2026-08-21T20:20:00+08:00
---

# TASK-0005 Goal Program

## Goal Mode

- `mode:` `active_goal`
- `trigger:` explicit operator request to implement TASK-0005 against its
  accepted ASCII prototype, operate the isolated v4 Notion demo, and rerun the
  proof.
- `budget:` no numerical budget supplied; continue while a ticket Done
  condition has a bounded, safe proof path.

## Metric Provider

- `provider:` hybrid — deterministic frozen assertions, receipt-backed v4
  Notion reads, browser-visible proof, and independent QA/review.
- `guards:` v4 namespace only; no real Kamdar source mutation; no guessed cost
  or cause; no delivery success without a receipt; no new dependency or
  parallel runner.
- `anti_metrics:` a green count without correct artifact/receipt evidence,
  created duplicate rows, invented external URLs, or visual polish that hides a
  source gap.

## Decision Backbone

`observe -> choose_next(objective, evidence, eligible_moves, remaining_budget) -> execute | diagnose | report_now | request_feedback | stop -> act -> verify -> write_back`

Implement inside the existing template-first runner, live edge, showcase, and
eval contract. Resolve the smallest failing Done condition before adding scope.

## Proof Policy

1. Contract/template/fixture validation.
2. Frozen run twice: arithmetic, cause honesty, comment-before-chase, and no
   duplicates.
3. Bounded v4 Notion operation and post-write re-read of root, databases,
   canonical rows, comments, applied results, and URLs.
4. Update the source-owned `workspace.hermes.md` with only receipt-backed v4
   links; run workspace validation.
5. Browser proof of width, type, expandable Daily/Weekly Markdown, real
   database/result links, and honest provider states.
6. Independent QA, visual, evidence, drift, and completion review; demo proof
   before completion.

## Context / Logging

Initial load is full ticket/program plus latest 80 progress lines; target 300
lines and hard-block above 400. Append `observation`, `evidence`, `learning`,
`decision`, `remaining_budget`, and `next_action` after every material phase.
Regenerate this packet when ticket scope, QA policy, or its inputs change.

## Stop Conditions

- `complete_when:` every Done / Proof checkbox, required independent receipts,
  and a current packet pass.
- `stop_now:` a write targets existing Kamdar records, credentials appear in an
  artifact, or a result link cannot be receipt-backed.
- `blocked_when:` the same non-inferable provider/tool gap persists through
  three documented diagnostic turns.
- `check_in_program:` not applicable — immediate implementation work.

## Final Checkpoint

Run the ordered critical-path checks, browser/visual proof, independent QA and
review, a concise demo capture, and ticket/progress writeback before
`stop_complete`. Grounding is local-only: this uses the existing vanilla
Node/HTML harness and the installed `ntn` Notion client; no new external API or
library decision is introduced.
