{
  "name": "kamdar-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "eval:judge:operated-w34": "npm --prefix evals/filesystem run judge:operated-w34",
    "eval:view:build": "npm --prefix evals/filesystem run view:build:operated-w34",
    "eval:view:serve": "npm --prefix evals/filesystem run view:serve"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
