import { mkdirSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

import { buildEvidenceModel } from "./model.mjs";

const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), "../..");

function args(argv) {
  const values = {};
  for (let index = 0; index < argv.length; index += 2) {
    const key = argv[index];
    const value = argv[index + 1];
    if (!key?.startsWith("--") || !value) throw new Error(`Expected --name value arguments; received ${key || "nothing"}.`);
    values[key.slice(2)] = value;
  }
  return values;
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

export function renderEvidenceHtml(model) {
  const modelJson = JSON.stringify(model).replaceAll("<", "\\u003c");
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Company OS evals</title>
<style>
:root{color-scheme:dark;--bg:#060606;--panel:#0d0d0d;--row:#111;--row-alt:#0b0b0b;--line:#272727;--line-strong:#3a3a3a;--ink:#d0d0ca;--muted:#777772;--peach:#f2ceb0;--lavender:#cec7ed;--mint:#b9ddcb;--pink:#e8b7c5;--yellow:#ead99d;--mono:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}*{box-sizing:border-box;scrollbar-color:#343434 #090909;scrollbar-width:thin}*::-webkit-scrollbar{width:8px;height:8px}*::-webkit-scrollbar-track{background:#090909}*::-webkit-scrollbar-thumb{border:2px solid #090909;background:#343434}html,body{height:100%}body{margin:0;overflow:hidden;background:var(--bg);color:var(--ink);font:12px/1.45 var(--mono)}button,a{font:inherit;color:inherit}button:focus-visible,summary:focus-visible,a:focus-visible{outline:1px solid var(--lavender);outline-offset:2px}.shell{height:100%;padding:18px 22px;display:grid;grid-template-rows:48px minmax(0,1fr);gap:10px}.topbar{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:0 12px;border:1px solid var(--line);background:var(--panel)}.topbar h1{margin:0;flex:0 1 auto;font-size:12px;font-weight:500;letter-spacing:.04em;text-transform:lowercase}.metrics{display:flex;align-items:center;justify-content:flex-end;gap:6px;color:var(--muted);font-size:9px}.metric-pill,.group-pill{display:inline-flex;align-items:center;gap:5px;white-space:nowrap}.metric-pill{padding:4px 7px;border:1px solid var(--line);border-radius:999px;background:#111;color:#aaa9a3}.group-pill{padding:3px 6px;border:1px solid var(--line);border-radius:999px;color:var(--muted);font-size:8px}.run-link{margin-left:2px;color:var(--muted);text-decoration:none;white-space:nowrap}.square{display:inline-block;width:10px;height:10px;flex:0 0 10px;border:1px solid rgba(255,255,255,.16)}.metric-square{width:8px;height:8px;flex-basis:8px;border:0}.tone-peach{background:var(--peach)}.tone-lavender{background:var(--lavender)}.tone-mint{background:var(--mint)}.tone-pink{background:var(--pink)}.tone-yellow{background:var(--yellow)}.workspace{min-height:0;display:grid;grid-template-columns:minmax(300px,40fr) minmax(520px,60fr);gap:10px}.list-panel,.inspector{min-height:0;border:1px solid var(--line);background:var(--panel)}.list-panel{overflow:auto}.list-head{position:sticky;top:0;z-index:4;display:grid;grid-template-columns:1fr 90px;padding:8px 11px;border-bottom:1px solid var(--line);background:#0a0a0a;color:var(--muted);font-size:9px;text-transform:lowercase}.feature-group{border-bottom:1px solid var(--line)}.feature-toggle{position:sticky;top:31px;z-index:3;width:100%;display:flex;align-items:center;justify-content:space-between;gap:10px;padding:8px 11px;border:0;border-bottom:1px solid var(--line);background:#0b0b0b;text-align:left;cursor:pointer}.feature-label{min-width:0;display:flex;align-items:center;gap:8px;font-weight:700}.toggle-glyph{width:8px;color:var(--muted);font-size:8px}.feature-rows.hidden{display:none}.feature-button{position:relative;width:100%;min-height:66px;display:grid;grid-template-columns:3px 11px minmax(0,1fr) auto;gap:10px;align-items:center;padding:9px 11px 9px 0;border:0;border-bottom:1px solid #1c1c1c;background:var(--row);text-align:left;cursor:pointer}.feature-button:nth-child(even){background:var(--row-alt)}.feature-button:hover,.feature-button[aria-current=true]{outline:1px solid var(--line-strong);outline-offset:-1px;background:#151515}.rail{align-self:stretch;background:transparent}.feature-button[aria-current=true] .rail{background:var(--peach)}.weekly .feature-button[aria-current=true] .rail{background:var(--lavender)}.feature-copy{min-width:0}.feature-copy b{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:11px}.feature-copy small{display:block;margin-top:5px;overflow:hidden;color:var(--muted);font-size:9px;text-overflow:ellipsis;white-space:nowrap}.status-pill{min-width:60px;padding:3px 6px;border-radius:2px;color:#171714;font-size:8px;font-weight:800;letter-spacing:.06em;text-align:center}.status-pill.pass{background:var(--mint)}.status-pill.fail{background:var(--pink)}.status-pill.unjudged{background:var(--lavender)}.inspector{overflow:auto}.inspector-head{position:sticky;top:0;z-index:3;display:flex;justify-content:space-between;gap:12px;padding:12px;border-bottom:1px solid var(--line);background:#0a0a0a}.kicker{margin:0 0 5px;color:var(--muted);font-size:9px}.title-row{display:flex;align-items:center;gap:8px}.inspector h2{margin:0;font-size:13px}.close{display:none;border:0;background:transparent;color:var(--muted);font-size:18px;cursor:pointer}.status-strip{display:flex;align-items:center;gap:8px;padding:10px 12px;border-bottom:1px solid var(--line)}.status-note{color:var(--muted);font-size:9px}.inspector section{margin:0;padding:12px;border-top:1px solid var(--line)}.inspector h3{margin:0 0 8px;color:#92928e;font-size:9px;letter-spacing:.08em;text-transform:uppercase}.task-copy{margin:0;color:#c9c9c3}.claim{margin:8px 0 0;color:#a4a4a0}.record{margin-top:7px;border:1px solid var(--line);background:#0a0a0a}.record>summary{min-height:42px;display:grid;grid-template-columns:10px minmax(0,1fr) auto;gap:9px;align-items:center;padding:7px 8px;background:#101010;cursor:pointer}.record[open]>summary{border-bottom:1px solid var(--line)}.record>summary:hover{background:#141414}.record-identity{min-width:0}.record-identity small{display:block;color:var(--muted);font-size:8px;text-transform:uppercase}.record-identity b{display:block;overflow:hidden;color:#deded8;font-size:10px;text-overflow:ellipsis;white-space:nowrap}.record-state{padding:3px 6px;border:1px solid var(--line-strong);color:#b7b7b1;font-size:8px;font-weight:800;letter-spacing:.05em;text-transform:uppercase;white-space:nowrap}.state-at-risk,.state-blocked{border-color:#766d47;color:var(--yellow)}.state-done,.state-final,.state-complete,.state-on-track,.state-processed{border-color:#426152;color:var(--mint)}.record pre{max-height:320px;margin:0;padding:9px;overflow:auto;color:#b7b7b2;font:9px/1.55 var(--mono);overflow-wrap:anywhere;white-space:pre-wrap}.case-list{margin:0;padding:0;list-style:none}.case-list li{padding:7px 0;border-bottom:1px solid #202020}.case-list li:last-child{border-bottom:0}.case-list b{display:block;color:#c9c9c3}.case-list small{display:block;margin-top:3px;color:var(--muted)}.evaluation-workbench{display:grid;grid-template-columns:1fr;gap:8px}.evaluation-output,.evaluation-criteria{min-width:0;border:1px solid var(--line);background:#090909}.evaluation-output>header,.evaluation-criteria>header{display:grid;gap:2px;padding:9px 10px;border-bottom:1px solid var(--line);background:#101010}.evaluation-output>header b,.evaluation-criteria>header b{color:#deded8;font-size:10px}.evaluation-output>header span,.evaluation-criteria>header span{color:var(--muted);font-size:8px}.output-list{display:grid;gap:6px;padding:8px}.output{display:grid;grid-template-columns:10px minmax(0,1fr) auto;gap:8px;align-items:center;padding:8px;border:1px solid #3e3a50;color:var(--lavender);text-decoration:none}.output:hover{background:#15131b}.output-copy{min-width:0}.output-copy b{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.output-copy small{display:block;margin-top:3px;color:var(--muted)}.empty{margin:8px;padding:9px;border:1px dashed var(--line);color:var(--muted)}.expected-summary{margin:0;padding:9px 10px;border-bottom:1px solid var(--line);color:#aaa9a4}.check-list{display:grid;gap:6px;margin:0;padding:8px;list-style:none}.check{display:grid;grid-template-columns:10px minmax(0,1fr) auto;gap:8px;align-items:start;padding:8px;border:1px solid var(--line);background:#0c0c0c}.check strong{padding:2px 5px;border:1px solid var(--line);font-size:7px;letter-spacing:.07em}.check.pass strong{color:var(--mint)}.check.fail{border-color:#573944;background:#110b0d}.check.fail strong{color:var(--pink)}.check.unjudged strong{color:var(--lavender)}
@media(max-width:900px){body{overflow:auto}.shell{height:auto;min-height:100%;padding:10px;grid-template-rows:auto minmax(calc(100vh - 92px),auto)}.topbar{min-width:0;min-height:54px;padding:8px 10px;overflow:hidden}.metrics{flex-wrap:wrap}.run-link{width:100%;text-align:right}.workspace{display:block;min-width:0}.list-panel{width:100%;min-height:calc(100vh - 92px);overflow-x:hidden}.inspector{position:fixed;inset:0;z-index:20;display:none;border:0}.inspector.open{display:block}.close{display:block;min-width:44px;min-height:44px}.feature-toggle{top:31px}}
@media(max-width:560px){.topbar{align-items:flex-start;flex-direction:column;gap:7px}.metrics{justify-content:flex-start}.run-link{width:auto;text-align:left}.feature-button{grid-template-columns:3px 11px minmax(0,1fr) 60px;gap:8px}}
</style>
</head>
<body>
<main class="shell">
  <header class="topbar"><h1>kamdar company os — w34</h1><div class="metrics" id="metrics"></div></header>
  <div class="workspace">
    <section class="list-panel" aria-label="Feature checks"><div class="list-head"><span>feature checks · grouped by workflow</span><span>result</span></div><div id="features"></div></section>
    <aside id="detail" class="inspector" aria-live="polite"></aside>
  </div>
</main>
<script type="application/json" id="evidence-model">${modelJson}</script>
<script>
const model=JSON.parse(document.getElementById('evidence-model').textContent);const metrics=document.getElementById('metrics');const featureList=document.getElementById('features');const detail=document.getElementById('detail');const esc=${escapeHtml.toString()};const tones=['peach','lavender','mint','pink','yellow'];function statusLabel(value){return value==='pass'?'PASSED':value==='fail'?'FAILED':'UNJUDGED'}function stateClass(value){return 'state-'+String(value||'').toLowerCase().replace(/[^a-z0-9]+/g,'-')}function cadenceLabel(value){return value==='daily'?'Unified Daily Review':value==='weekly'?'Weekly Operating Review':'Meeting Intake'}const metricData=[['mint',model.metrics.features.passed+'/'+model.metrics.features.total+' features'],['lavender',model.metrics.cases.total+'/'+model.metrics.cases.total+' cases'],['peach',model.metrics.checks.passed+'/'+model.metrics.checks.total+' checks'],['yellow',model.metrics.outputs.total+'/'+model.metrics.outputs.total+' outputs']];metrics.innerHTML=metricData.map(([tone,label])=>'<span class="metric-pill"><i class="square metric-square tone-'+tone+'"></i>'+esc(label)+'</span>').join('')+(model.rootOutputUrl?'<a class="run-link" href="'+esc(model.rootOutputUrl)+'" target="_blank" rel="noreferrer">operated—w34—2026—08—26 ↗</a>':'');function render(index,openMobile=true){const f=model.features[index];document.querySelectorAll('.feature-button').forEach((button,i)=>button.setAttribute('aria-current',String(i===index)));const tone=tones[index%tones.length];const sources=f.sources.map((source,sourceIndex)=>'<details class="record"><summary><i class="square tone-'+tones[sourceIndex%tones.length]+'"></i><span class="record-identity"><small>'+esc(source.kind)+'</small><b>'+esc(source.name)+'</b></span><span class="record-state '+stateClass(source.status)+'">'+esc(source.status||'—')+'</span></summary><pre>'+esc(JSON.stringify(source.record,null,2))+'</pre></details>').join('');const cases='<ul class="case-list">'+f.cases.map(row=>'<li><b>'+esc(row.title)+'</b><small>'+esc(row.expectedOutput)+'</small></li>').join('')+'</ul>';const outputs=f.outputs.length?'<div class="output-list">'+f.outputs.map(row=>'<a class="output" href="'+esc(row.url)+'" target="_blank" rel="noreferrer"><i class="square tone-lavender"></i><span class="output-copy"><b>'+esc(row.label)+' ↗</b><small>'+esc(row.kind)+(row.accessNote?' · '+esc(row.accessNote):'')+'</small></span></a>').join('')+'</div>':'<p class="empty">No human-facing output link was recorded for this feature.</p>';const checks='<ul class="check-list">'+f.assertions.map(row=>'<li class="check '+row.status+'"><i class="square tone-'+(row.status==='pass'?'mint':row.status==='fail'?'pink':'lavender')+'"></i><span>'+esc(row.assertion)+'</span><strong>'+esc(row.status==='pass'?'MET':row.status==='fail'?'MISS':'PENDING')+'</strong></li>').join('')+'</ul>';detail.innerHTML='<header class="inspector-head"><div><p class="kicker">'+esc(cadenceLabel(f.cadence))+'</p><div class="title-row"><i class="square tone-'+tone+'"></i><h2>'+esc(f.name)+'</h2></div></div><button class="close" type="button" aria-label="Close inspector">×</button></header><div class="status-strip"><span class="status-pill '+f.status+'">'+statusLabel(f.status)+'</span><span class="status-note">'+esc(f.statusNote)+'</span></div><section><h3>Task</h3><p class="task-copy">'+esc(f.claim)+'</p></section><section><h3>Source input · seed JSON</h3>'+sources+'</section><section><h3>Test cases</h3>'+cases+'</section><section><h3>Assertion review</h3><div class="evaluation-workbench"><article class="evaluation-output"><header><b>Actual agent output</b><span>Human-facing artifacts from the operated run</span></header>'+outputs+'</article><article class="evaluation-criteria"><header><b>Expected criteria</b><span>'+f.assertions.filter(row=>row.status==='pass').length+'/'+f.assertions.length+' met by the feature judge</span></header><p class="expected-summary">'+esc(f.claim)+'</p>'+checks+'</article></div></section>';detail.querySelector('.close').addEventListener('click',()=>{detail.classList.remove('open');document.body.classList.remove('inspector-open')});detail.scrollTop=0;if(openMobile){detail.classList.add('open');document.body.classList.add('inspector-open')}}function group(cadence,label,tone){const indexes=model.features.map((feature,index)=>({feature,index})).filter(row=>row.feature.cadence===cadence);if(!indexes.length)return;const section=document.createElement('section');section.className='feature-group '+cadence;section.innerHTML='<button class="feature-toggle" type="button" aria-expanded="true"><span class="feature-label"><span class="toggle-glyph">▼</span><i class="square tone-'+tone+'"></i><span>'+esc(label)+'</span></span><span class="group-pill">'+indexes.filter(row=>row.feature.status==='pass').length+'/'+indexes.length+' passed</span></button><div class="feature-rows"></div>';const rows=section.querySelector('.feature-rows');indexes.forEach(({feature,index})=>{const button=document.createElement('button');button.className='feature-button';button.type='button';button.innerHTML='<span class="rail"></span><i class="square tone-'+tone+'"></i><span class="feature-copy"><b>'+esc(feature.name)+'</b><small>'+feature.assertions.length+' required checks '+(feature.status==='pass'?'passed.':feature.status==='fail'?'failed.':'awaiting judge evidence.')+'</small></span><span class="status-pill '+feature.status+'">'+statusLabel(feature.status)+'</span>';button.addEventListener('click',()=>render(index,true));rows.appendChild(button)});section.querySelector('.feature-toggle').addEventListener('click',event=>{const open=event.currentTarget.getAttribute('aria-expanded')==='true';event.currentTarget.setAttribute('aria-expanded',String(!open));event.currentTarget.querySelector('.toggle-glyph').textContent=open?'▶':'▼';rows.classList.toggle('hidden',open)});featureList.appendChild(section)}group('daily','Unified Daily Review','peach');group('weekly','Weekly Operating Review','lavender');group('meeting','Meeting Intake','mint');render(0,false);
</script>
</body>
</html>`;
}

export function buildStaticEvidenceViewer({ outDir, evidencePath = null, dailyRunRoot = null, weeklyRunRoot = null, meetingRunRoot = null }) {
  const model = buildEvidenceModel({ projectRoot, evidencePath, dailyRunRoot, weeklyRunRoot, meetingRunRoot });
  mkdirSync(outDir, { recursive: true });
  writeFileSync(resolve(outDir, "model.json"), `${JSON.stringify(model, null, 2)}\n`);
  writeFileSync(resolve(outDir, "index.html"), renderEvidenceHtml(model));
  return model;
}

if (process.argv[1] === fileURLToPath(import.meta.url)) {
  const values = args(process.argv.slice(2));
  const outDir = resolve(projectRoot, values.out || "evals/viewer/dist");
  const model = buildStaticEvidenceViewer({
    outDir,
    evidencePath: values.evidence ? resolve(projectRoot, values.evidence) : null,
    dailyRunRoot: values["daily-run"] ? resolve(values["daily-run"]) : null,
    weeklyRunRoot: values["weekly-run"] ? resolve(values["weekly-run"]) : null,
    meetingRunRoot: values["meeting-run"] ? resolve(values["meeting-run"]) : null,
  });
  process.stdout.write(`${JSON.stringify({ outDir, metrics: model.metrics })}\n`);
}
