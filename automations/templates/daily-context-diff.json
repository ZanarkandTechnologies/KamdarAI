{
  "artifact_type": "kamdar-daily-context-diff",
  "artifact_version": "0.2.0",
  "context_id": "daily-context-{{LOCAL_DAY}}",
  "local_day": "{{YYYY-MM-DD}}",
  "evidence_window": {
    "start": "{{ISO-8601 in company timezone}}",
    "end": "{{ISO-8601 in company timezone}}"
  },
  "collector": {
    "run_id": "{{stable run ID}}",
    "provider_effects": {"performed": false}
  },
  "source_manifest": [
    {
      "source_key": "{{workspace Daily source catalog key}}",
      "status": "{{fetched | unavailable | skipped}}",
      "source_url": "{{resolved configured source link}}",
      "collection_scope": "{{bounded query or selection rule}}",
      "collected_at": "{{ISO-8601 timestamp}}",
      "record_count": 0,
      "source_ids": ["{{retained source IDs from this source}}"],
      "gap": "{{null or exact unavailable/skipped reason}}"
    }
  ],
  "projects": [
    {
      "id": "{{PROJECT_ID}}",
      "source_id": "{{PROJECT_SOURCE_ID}}",
      "source_url": "{{stable Project URL or source reference}}",
      "name": "{{Project name}}",
      "owner_person_id": "{{PERSON_ID | null}}",
      "project_knowledge": "{{current Project knowledge section}}",
      "this_weeks_attention": "{{current weekly checklist}}",
      "weekly_attention_reset": {
        "requested": false,
        "week": "{{YYYY-Www | null}}",
        "reason": "{{explicit approved-planning reset reason | null}}",
        "source_id": "{{PROJECT_SOURCE_ID}}"
      }
    }
  ],
  "work_items": [
    {
      "id": "{{WORK_ITEM_ID}}",
      "source_id": "{{WORK_SOURCE_ID}}",
      "source_url": "{{stable Work URL or source reference}}",
      "project_id": "{{PROJECT_ID | null}}",
      "record_type": "{{Task | Feature | Issue | Meeting}}",
      "full_page_read": true,
      "owner_person_id": "{{PERSON_ID | null}}",
      "status": "{{current status}}",
      "due_date": "{{YYYY-MM-DD | null}}",
      "last_meaningful_update": "{{YYYY-MM-DD | null}}",
      "blocker": "{{known blocker | null}}",
      "cause": {"value": "{{known cause | null}}", "confidence": "{{high | medium | low | unknown}}"},
      "plan_actual": {"currency": "{{ISO currency | null}}", "estimated_amount": null, "actual_amount": null},
      "documentation": {
        "known_context": "{{concise known record context}}",
        "mapped_field_state": {"{{field}}": "{{present | missing | vague}}"},
        "update_location": ["{{property or page section}}"]
      },
      "evidence": ["{{source-backed fact}}"]
    }
  ],
  "meetings": [
    {
      "id": "{{MEETING_ID}}",
      "source_id": "{{MEETING_SOURCE_ID}}",
      "source_url": "{{stable Meeting URL or source reference}}",
      "project_id": "{{PROJECT_ID | null}}",
      "statements": ["{{attributed/source-backed statement}}"],
      "decision_observation": {
        "choice": "{{concrete choice | null}}",
        "authority": "{{decision authority | unknown}}",
        "evidence": ["{{source-backed decision fact}}"]
      },
      "workflow_observation": {
        "workflow_name": "{{observed workflow name | null}}",
        "trigger": "{{observed trigger | unknown}}",
        "actors_and_handoff": "{{observed actors and handoff | unknown}}",
        "ordered_steps": ["{{observed step}}"],
        "systems": ["{{observed system}}"],
        "frequency_and_volume": "{{observed frequency and volume | unknown}}",
        "active_and_wait_time": "{{measured timing | unknown}}",
        "exceptions": ["{{observed exception}}"],
        "output": "{{observed output | unknown}}",
        "confidence": "{{high | medium | low}}",
        "measurement_gaps": ["{{fact still requiring measurement}}"]
      },
      "review_condition": "{{what would revisit the finding | null}}"
    }
  ],
  "people": [
    {
      "id": "{{PERSON_ID}}",
      "source_id": "{{PERSON_SOURCE_ID}}",
      "name": "{{name}}",
      "preferred_contact_channel": "{{channel | null}}",
      "approved_contact_channels": ["{{approved channel}}"],
      "approved_contact_endpoint_ref": "{{approved endpoint reference | null}}",
      "contact_instructions": "{{route instruction | null}}"
    }
  ]
}
